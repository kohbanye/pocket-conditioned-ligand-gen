"""Task metric implementations (ported verbatim from the source-repo paper notebooks).

Each function is a small, dependency-light re-implementation of a metric defined
in ``pocket-conditioned-ligand-gen/notebooks/paper_*.py`` so this repo can
recompute every headline number from per-sample dumps and is covered by unit
tests.
"""
